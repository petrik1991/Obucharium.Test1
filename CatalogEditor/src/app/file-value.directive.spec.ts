import { FileValueDirective } from './file-value.directive';

describe('FileValueDirective', () => {
  it('should create an instance', () => {
    const directive = new FileValueDirective();
    expect(directive).toBeTruthy();
  });
});
